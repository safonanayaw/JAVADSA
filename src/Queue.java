//Queue store object in FIFO (First come, first out).
//A collection design to holding data prior to processing
//Enqeuing(add) is adding item to the tail/ offer()
//Dequeing(remove) is removing item from the dead/ poll()
//peek() read the element at the tail
//use CASE keyboard buffer (lettered appear in order of typed),
// print queue(print jobs should be completed in order)
//used in LinkedList, Priority queue, breadth-first-search
public class Queue {
}
